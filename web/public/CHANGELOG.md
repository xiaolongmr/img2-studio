# 更新记录

## 2026-05-26

### 新增
- 新增「更新记录」页面 `/changelog`，直接渲染本地 `CHANGELOG.md`。
- 左侧导航新增「更新记录」入口，方便查看每次迭代内容。
- 更新记录采用 Markdown 维护，可持续追加版本日志。

### 优化
- 图片工作台路由改为懒加载，减少跨页面跳转首屏阻塞。
- 历史会话详情改为延后挂载，进入工作台时先保证首屏可交互。
- 对话区初始仅渲染更少历史 turn，降低大会话切换时的渲染压力。

### 修复
- 修复图片接口响应格式解析的空值访问问题：`config.storage` 缺失时不再报错。
- 修复引用插入逻辑中的光标位置兼容细节，避免覆盖错误。
- 修复「图片工作台 / 提示词库 / 设置」等页面导航文案一致性问题。
